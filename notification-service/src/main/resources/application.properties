# Server Configuration
server.port=8083

# Application Name
spring.application.name=notification-service

# PostgreSQL Configuration
spring.datasource.url=jdbc:postgresql://postgres:5432/notificationdb
spring.datasource.username=postgres
spring.datasource.password=Sparkle.2002@@
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# RabbitMQ Configuration
spring.rabbitmq.host=rabbitmq
spring.rabbitmq.port=5672
spring.rabbitmq.username=guest
spring.rabbitmq.password=guest

# ✅ ACTUATOR CONFIGURATION (AJOUTEZ CECI)
management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always
management.health.rabbitmq.enabled=true

# Logging
logging.level.com.leaveflow=DEBUG
logging.level.org.springframework.amqp=DEBUG